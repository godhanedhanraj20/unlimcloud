
import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import Header from "@/components/Header";
import Footer from "@/components/Footer";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "UnlimCloud Desktop App",
  description: "Unofficial Desktop Application for UnlimCloud - Secure cloud storage using your Telegram ID",
  keywords: ["UnlimCloud", "cloud storage", "desktop app", "telegram", "file management"],
  authors: [{ name: "inulute" }],
  openGraph: {
    title: "UnlimCloud - The Unofficial Web and Desktop App",
    description: "A fast, modern, and unofficial web client for your UnlimCloud storage.",
    images: ["/assets/unlim-clear.png"],
  },
};

export const viewport = {
  width: "device-width",
  initialScale: 1,
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-gray-50 text-gray-800`}
      >
        <Header />
        <main className="min-h-screen">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  );
}
